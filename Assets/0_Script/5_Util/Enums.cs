
public enum GridState { NONE = 0, START, END, CAMERA }

public enum BoxDir { FORWARD = 0, BOTTOM, BACK, TOP, LEFT, RIGHT }

public enum EffectClip { BASIC_JUMP =0, D_JUMP, H_JUMP}

public enum BGMClip { MAIN_BGM = 0, STAGE_1,  STAGE_2}